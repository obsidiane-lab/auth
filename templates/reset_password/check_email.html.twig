{% extends 'auth/page_base.html.twig' %}

{% block auth_component %}
  <h1 class="mb-6 text-2xl font-semibold">Vérifiez votre boîte mail</h1>
  <p class="text-muted-foreground">
    Si un compte correspond à cette adresse, un e-mail contenant un lien de réinitialisation vous a été envoyé.
    Ce lien expirera dans environ {{ tokenLifetime }} minute(s).
  </p>
  <p class="mt-6 text-sm text-muted-foreground">
    <a class="text-primary underline" href="{{ path('auth_login_page') }}">Retour à la connexion</a>
  </p>
{% endblock %}

